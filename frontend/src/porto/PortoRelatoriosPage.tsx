import { useState } from 'react'
import { baixarRelatorioPorto } from '../api/porto'

export default function PortoRelatoriosPage(){const [erro,setErro]=useState('');async function baixar(formato:'excel'|'pdf'){setErro('');try{await baixarRelatorioPorto(formato)}catch(e){setErro((e as Error).message)}}return <div className="page-enter"><header className="page-heading"><div><span className="eyebrow">Porto Seguro</span><h1>Relatórios Porto</h1><p>Exportações gerenciais sem expor dados pessoais desnecessários.</p></div></header>{erro?<div className="form-alert">{erro}</div>:null}<section className="panel report-actions"><article><h2>Relatório consolidado</h2><p>OPs, serviços, conciliação, pendências e agrupamentos.</p><div className="heading-actions"><button className="button button-ghost" onClick={()=>void baixar('pdf')}>Baixar PDF</button><button className="button button-primary" onClick={()=>void baixar('excel')}>Baixar Excel</button></div></article><article><h2>Relatório por OP</h2><p>Abra uma ordem de pagamento e exporte sua composição individual em sete abas.</p></article></section></div>}
